---
govready_repo: "https://github.com/GovReady/govready.git"
govready_repo_local_destination: "/usr/local/govready"
govready_executable_src: "/usr/local/govready/govready"
govready_executable_link: "/usr/local/bin/govready"
govready_myfisma_directory: "/home/vagrant/myfisma"
govready_myfisma_scans_directory: "{{ govready_myfisma_directory }}/scans"
govready_myfisma_scap_directory: "{{ govready_myfisma_directory }}/scap"
govready_myfisma_configuration_file: "{{ govready_myfisma_directory }}/GovReadyfile"
govready_scap_content_directory: "/usr/local/scap-security-guide/RHEL/7/dist/content/"
govready_share_directory: "/home/vagrant/v-share"
govready_share_scans_directory: "{{ govready_share_directory }}/scans"
